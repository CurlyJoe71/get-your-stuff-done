import { Button } from '@mui/material';
import React from 'react';

const Home = () => {
    return (
        <div>
            <Button>Test Button</Button>
        </div>
    )
};

export default Home;